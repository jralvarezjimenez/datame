rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: any authenticated user has full access
    // TODO: Tighten for production with RBAC
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
